run installpre-commit:
  parallel: true
  commands:
    eslint:
      run: yarn lint
      glob: "*.{js,jsx,ts,tsx}"
    prettier:
      run: yarn prettier --write
      glob: "*.{json,css,md}"
    commitlint:
      run: npx --no-install commitlint --edit ${1}
      files: $1
