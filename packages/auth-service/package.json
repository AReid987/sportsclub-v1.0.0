{
  "name": "@sportsclub/auth-service",
  "version": "0.1.0",
  "private": true,
  "description": "Authentication and User Service (FastAPI)",
  "scripts": {
    "dev": "uvicorn main:app --reload --port 8001 --app-dir src",
    "lint": "ruff check . && ruff format --check . && mypy ."
  }
}
